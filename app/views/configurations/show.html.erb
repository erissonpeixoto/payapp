<% unless notice.nil? %>
  <div class="row">
    <div class="col s12 m12">
      <div class="card-panel green accent-2">
        <span id="notice" class="black-text">
          <i class="material-icons">check</i>
          <%= notice %>
        </span>
      </div>
    </div>
  </div>
<% end %>

<p>
  <strong>Salário:</strong>
  <%= number_to_currency(@configuration.salary, unit: "R$ ", separator: ",", delimiter: ".") %>
</p>


<%= link_to 'Editar', edit_configuration_path(@configuration), class: 'waves-effect waves-light btn blue-grey' %> |
<%= link_to 'Voltar', configurations_path, class: 'waves-effect waves-light btn blue-grey' %>
